repos:
  - repo: local
    hooks:
      - id: pytest-on-push
        name: Run pytest on push
        entry: pytest -q
        language: python
        additional_dependencies: 
          - -r requirements.txt
        stages: [push]
        types: [python]